#include "main.h"

/**
 * _isupper - Check if a letter is upper.
 * @x: number to be checked
 * Return: return type
 */

int _isupper(int x)

{

	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
